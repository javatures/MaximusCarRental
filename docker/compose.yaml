version: '3.7'

services:
  db:
    build:
      context: .
      dockerfile: postgres.dockerfile
    image: db
    container_name: db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_HOST: db
    ports:
      - 5432:5432